// Minimal OpenNext config for Cloudflare adapter.
// Provide a non-empty default export object to satisfy the CLI.
// The adapter defaults to using `.open-next` as output directory.
module.exports = {
  default: {}
}

